/*
 * HomeRouteScreen Messages
 *
 * This contains all the text for the HomeRouteScreen component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.screens.HomeRouteScreen';

export default defineMessages({
  exitMessage: {
    id: `${scope}.exitMessage`,
    defaultMessage: 'Press again to exit app',
  },
});
