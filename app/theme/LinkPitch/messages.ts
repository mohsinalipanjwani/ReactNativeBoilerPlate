/*
 * LinkPitch Messages
 *
 * This contains all the text for the LinkPitch component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.screens.LinkPitch';

export default defineMessages({
  pitch: {
    id: `${scope}.pitch`,
    defaultMessage: ' in',
  },
});
